# 阶段2 数字型接入 — 概览

> 范围：把「先做有的」4 个数字型彩种（排列5 / 福彩3D / 排列3 / 七星彩）接进预测链路。
> 硬约束：双色球 / 大乐透 行为 100% 零回归。

## 已交付能力

| 彩种 | 位数 | 数据期数 | 预测 | 统计 | 报告/图表 |
|------|------|----------|------|------|-----------|
| 排列5 | 5（万/千/百/十/个） | 7684 | ✅ | ✅ | ✅ |
| 福彩3D | 3（百/十/个） | 7718 | ✅ | ✅ | ✅ |
| 排列3 | 3（百/十/个） | 7686 | ✅ | ✅ | ✅ |
| 七星彩 | 7（第1–7位） | 3284 | ✅ | ✅ | ✅ |

## 架构改动（红/蓝零回归）

- **config.py**：数字型彩种用 `zones` 描述（按位有序、0–9、choose=1）。
- **data/schema.py**：`DrawRecord.zone_numbers` 为唯一真相；`红球`/`蓝球` 作为属性兼容。
- **data/loader.py**：新彩种读 `号码1..N` 列映射到各 Zone；旧红/蓝逻辑字节级保留。
- **pipeline/statistics.py**：频率/遗漏按 `schema.zones` 返回；旧红/蓝结构不变。
- **prediction/engine.py**：`PredictSet` 以 `zones` 为主；新增通用预测块 `_predict_generic`（三策略 + 4 维置信度 + 去重），`predict()` 顶部按「有无红/蓝区」分流。
- **prediction/reporter.py** + **cli.py**：图表/报告/打印/统计全部「区驱动」通用化，红/蓝只是分区名恰好为红球/蓝球，自然兼容。
- **train/engine.py**：入口守卫——数字型训练未实现时抛清晰 `NotImplementedError`。

## 修复的关键缺陷

1. 通用策略函数返回 `dict` 却被当 `PredictSet` 用 → 改为返回 `PredictSet`（原崩溃）。
2. 历史命中率恒为 1.0（数字型每区仅 0–9，top-10=全量）→ 改为取每区一半为热号阈值，命中率现 ~0.44–0.51。
3. reporter 写死 `ps["红球"]` → 改为遍历 `ps["号码"]` 分区（含一次误删函数尾段已补回）。

## 验证结果

- `scripts/regression_check.py` ✅ 双色球/大乐透 特征向量逐元素一致、预测行为零破坏。
- `scripts/smoke_new_lotteries.py` ✅ 4 彩种加载 + 三策略预测无崩溃。
- `cli.py predict 排列5` ✅ 输出 5 位号码并生成 report.md / charts / prediction.json。
- `cli.py predict 双色球` ✅ 仍显示「红球 | 蓝球」列（向后兼容）。
- `cli.py train 排列5` ✅ 清晰提示「数字型训练尚未实现」。

## 已知遗留（后续阶段）

- **七星彩数据过期**：CSV 仅到 2025-12-30，本地数据过期触发「马后炮」保护，pending 预测被拒记录（预测本身正常）。需刷新数据。
- **Web 层未适配**：web 反馈/战绩/号码球渲染深度写死红/蓝，新彩种走 Web 会显示异常（Phase 3 级改造）。
- **数字型训练**未实现（ML 标签/损失函数按红/蓝写死），仅支持预测。
- **pipeline 命令**（step1–5）仍依赖红/蓝特征，新彩种运行会崩——非本次范围。
